__pycache__/
*.pyc
.DS_Store
.venv/
venv/
*.egg-info/
replays/*.json
replays/*.mp4
tools/asset_pipeline/venv/
web/assets_animated/*.png
web/assets_animated/*.mp4
# Keep .gitkeep in assets if needed, but ignore generated ones if they are reproducible
# For now, let's keep generated assets committed as they are part of the demo
